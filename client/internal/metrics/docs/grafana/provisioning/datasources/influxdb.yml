apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    editable: true
    jsonData:
      version: Flux
      organization: netbird
      defaultBucket: metrics
    secureJsonData:
      token: netbird-metrics-token